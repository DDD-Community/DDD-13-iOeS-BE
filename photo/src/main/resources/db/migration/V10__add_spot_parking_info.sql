-- ============================================================
-- V10: 스팟 주차 정보 컬럼 추가
-- IF NOT EXISTS 사용으로 신규/기존 환경 모두 안전하게 실행됨
-- ============================================================

ALTER TABLE spots ADD COLUMN IF NOT EXISTS parking_info VARCHAR(255);
